============
Installation
============

At the command line::

    $ easy_install django-message-broker

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-mb
    $ pip install django-message-broker
